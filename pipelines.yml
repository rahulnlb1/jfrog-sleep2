pipelines:
- name: my_pipe
  decorators:
  - name: my_dec1
    execution:
      onBeforeStart:
      - echo "before start 1"
      onAfterStart:
      - echo "after start 1"
      onBeforeComplete:
      - echo "before complete 1"
      onBeforeSuccess:
      - echo "before success 1"
  - name: my_decorator
    execution:
      onBeforeStart:
      - echo "before start"
      onAfterStart:
      - echo "after start"
      onBeforeComplete:
      - echo "before complete"
      onBeforeSuccess:
      - echo "before success"
  steps:
  - name: step1
    type: Bash
    decorators:
      - name: my_decorator 
      - name: my_dec1 
    execution:
      onStart: 
      - echo "on start"
      onComplete:
      - echo "on complete"  
